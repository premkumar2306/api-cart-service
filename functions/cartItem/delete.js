'use strict';
module.exports.delete = async (event, context) => {
    // TODO:
    // Get the cart
    // delete the item after the validtionis correct
    // save the cart back
    return { statusCode: 200, body: JSON.stringify({ data: 'success' }) };
};