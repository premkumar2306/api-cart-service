'use strict';
module.exports.update = async (event, context) => {
    // TODO:
    // Get the cart
    // update item after the validtionis correct
    // save the cart back
    return { statusCode: 200, body: JSON.stringify({ data: 'success' }) };
};